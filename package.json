{
  "name": "pokevault-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm --prefix backend install",
    "start": "node backend/src/server.js",
    "build": "npm --prefix backend install",
    "seed": "node backend/src/seedProducts.js"
  }
}
